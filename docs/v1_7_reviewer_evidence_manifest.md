# v1.7.9 Reviewer Evidence Manifest

Each rung should produce both full system output and a compressed reviewer state.

## Required layers

```text
full output report
compressed summary
visual top card
machine-readable row / JSON
bundle / handoff-ready archive
```

## Rungs

```text
triad27 = 3^3 local expression weather
deep81 = 3^4 perturbation / late-shock bridge
wide243 = 3^5 temporal-depth / time-axis stress
```

## What the manifest protects

- no missing evidence includes;
- no false handoff;
- no table-only front-page collapse;
- no all-weather first-run masking;
- core-question closeout now lives in v1.7.10 and remains bounded.

Native witness remains `C_Z = min(D, P, R, B)`.
