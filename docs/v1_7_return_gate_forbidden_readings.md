# v1.7.1 — Return Gate Forbidden Readings

These readings are blocked in v1.7.

| forbidden reading | why blocked | replacement |
|---|---|---|
| `Gamma` is observed return | `Gamma = D * P * R`; `B` is observed return | `Gamma` makes return possible; `B` witnesses return |
| zero crossing equals return | noise, reset traps, and collapse can cross zero | return requires memory, continuity, and persistence |
| return-potential is physical gravity | repo evidence is controlled synthetic-field software | call it structural return-potential only |
| return debt is failure | return debt is structured zero | hold the pressure as `0` until earned or demoted |
| missing return can be averaged away | native witness is weakest-gate coherence | `C_Z = min(D, P, R, B)` |
| v1.7.1 answers the core question | this gate is trace lock only | core answer waits for later v1.7 evidence gates |

No crown without return. No return without memory. No witness without boundary.
