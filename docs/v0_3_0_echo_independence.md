# v0.3.0-alpha — Echo-Independence Engine

This release begins the v0.3.x line because the project has moved from gate construction into adversarial witness: distinguishing earned relation from borrowed relation.

The core zero-gate remains intact. v0.3.0 adds matrix-level echo-independence outputs:

- `matrix_echo_independence_summary.csv`
- `matrix_echo_independence_read.md`

The engine reads whether candidate expression survives relation-minus, relation-zero, and relation-plus weather.

The first target is the field-echo wound: traps that become +1 by borrowing field coherence.

This is still toy simulation evidence. It does not prove cosmology or physics. It tests whether the zero-gate framework can avoid false expression when the candidate field becomes wider and more deceptive.
