# v1.7.2-alpha — Lane Visibility Decision Rules

**Version:** `v1.7.2-alpha`  
**Status:** decision rules before baseline matrix  
**Native witness:** `C_Z = min(D, P, R, B)`

## Rule table

| rule | pass condition | hold / fail condition |
|---|---|---|
| earned-one visibility | earned expressers remain visible as final `+1` | dead-safe refusal if zero false crowns occur only by refusing all crowns |
| raw pressure visibility | raw expression pressure remains reported before final witness | final outputs hide what was filtered |
| latent overcrown integrity | later evidence reproduces latent overcrown or v1.7.9 narrows the claim | seed-sensitive latent lane stays in full `+1` closeout without caveat |
| relation debt specificity | relation debt has relation-specific evidence surface | relation debt is only role-label recounting or generic hold |
| return debt specificity | return debt keeps `Gamma` / observed `B` separation | return debt becomes zero-crossing theater or generic hold |
| false-one safety | raw false-one pressure is visible and demoted; final false-one crowns remain `0` | any trap or false pressure receives final `+1` |
| baseline gate forward | lane set is locked before v1.7.3 | baselines are asked to beat a blurry target |

## Closeout consequence

`v1.7.9-alpha` must use these rules when deciding `+1`, `0`, or `-1` for the core question.

A beautiful lane table is not proof. It is the map of where proof must walk without falling into a ditch wearing reviewer shoes.
