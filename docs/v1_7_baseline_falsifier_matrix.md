# v1.7.3-alpha — Baseline and Ablation Falsifier Matrix

**Version:** `v1.7.3-alpha`  
**Gate:** Baseline and Ablation Falsifier Matrix  
**Native witness:** `C_Z = min(D, P, R, B)`

## Core question

> Can a final trinary witness distinguish earned-one from raw expression pressure, latent overcrown, relation debt, return debt, and false-one pressure under controlled synthetic-field adversarial weather?

`v1.7.3-alpha` does not close the question. It names the weaker witnesses that must fail before the native final trinary witness can later be trusted.

## Required baseline enemies

| enemy | what it removes / collapses | expected wound |
|---|---|---|
| `raw_expression_only` | final witness stack | raw pressure is crowned too early |
| `binary_raw_or_fail` | trinary output | structured zero disappears |
| `dead_safe_no_crown` | all crowns | earned-one is lost |
| `average_gate_raw` | weakest-gate law | strong gates average away a missing gate |
| `no_return_gate_raw` | observed return `B` | return debt gets crowned |
| `no_relation_gate_raw` | relation `R` | under-owned relation gets crowned |
| `no_lineage_or_return_depth_witness` | temporal lineage / return-depth | local pressure is crowned before maturity |
| `no_echo_independence_witness` | borrowed-relation guard | field echo becomes fake earned-one |
| `no_zero_hold_witness` | structured zero hold | latent/debt pressure is promoted or erased |

## Fatal failure

```text
A simpler witness explains the same result.
```

The most dangerous counterfeit is dead-safe safety:

```text
0 final false-one crowns + 0 real earned-one = not intelligence.
```

## Boundary

This gate is a falsifier matrix. It adds no new heavy evidence crown, starts no manuscript v2, makes no role-blind discovery claim, and preserves `C_Z = min(D, P, R, B)`.

Next gate: `v1.7.4-alpha — Perturbation Spectrum Witness`.
