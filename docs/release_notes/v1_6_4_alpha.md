# v1.6.4-alpha — Four-Gate Reconciliation / Return-Adversary Audit

## Status

Foundation correction and reconciliation gate. No native math change. No role-blind discovery claim.

## Delivered

- `src/zerogate_sim/four_gate_reconciliation_report.py`;
- console script `zerogate-four-gate-reconciliation`;
- `docs/four_gate_reconciliation.md`;
- `docs/release_notes/v1_6_4_alpha.md`;
- tests for native four-gate / historical three-corpus separation, forbidden claim-language detection, report output, and Zenodo correction-note source text;
- final-output readout repair so final false-one crowns are computed from rows instead of hardcoded in report text.

## What this does

`v1.6.4-alpha` names the first-alpha wound without backdating evidence:

```text
historical first-alpha = three dedicated adversarial corpora
native mechanism = four gates, including observed return B
later evidence = dedicated return-adversary coverage and controlled four-gate runs
```

Correct public sentence:

```text
The historical first-alpha proof is a three-corpus pre-return adversarial proof with return measured as a native gate and final witness requirement; later controlled evidence adds dedicated return-adversary coverage.
```

## Boundary

This is not a new proof harness and not a stronger role-blind result. It does not change:

```text
C_Z = min(D, P, R, B)
```

It repairs the claim lane before the shadow line moves to holdout evaluation.

## Next

`v1.6.5-alpha` performs the held-out `deep81` / `wide243` role-stripped evaluation after this correction gate.
