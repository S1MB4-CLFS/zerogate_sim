# v1.8.0-alpha — Observable Schema and Label Firewall

This local alpha checkpoint implements the first role-free repair boundary
after the v1.7.11 Evidence Integrity Correction.

## Added

- exact seven-field observable allowlist;
- separate observable, hash-bound join-key, and hash-bound label artifacts;
- immutable callback arguments without identifiers or labels;
- canonical prediction file, freeze manifest, and pre-label receipt;
- expected split-manifest hash required before label join;
- single-snapshot hash-and-parse verification;
- reverse-order repeat and row-permutation controls;
- exact duplicate/missing/extra join refusal;
- forbidden-field, label-permutation, identifier-renaming, tamper, and failure-capability tests;
- permanent Coding Economics and version-surface laws in Protocol v3;
- explicit manuscript-v2 empirical-readiness gate.

## Decision

`LOCAL_GREEN_FIREWALL_ONLY` when all executable canaries and tests pass.

Scientific authority remains `v1.7.11-alpha`, `0 / HOLD`.

## Not included

- scientific scorer or thresholds;
- predictor code/config provenance binding;
- frozen holdout reveal;
- unseen-generator empirical result;
- DTA transfer;
- manuscript v2 prose;
- merge, tag, push, PR, release, or archive upload.

## Next checkpoint

`v1.8.1-alpha — Lineage-Bearing Predictor Package and Development-Only
Preregistration`, only after explicit authorization to begin scorer/threshold
design.

## CI portability correction

- Tamper regression now changes prediction bytes directly instead of relying on
  platform-specific newline translation.
- The repository protocol surface is always tested; byte equality with the
  optional workspace-level protocol copy is tested only when that external copy
  exists.
