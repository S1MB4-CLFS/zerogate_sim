# v1.6.13-alpha — Shadow route historical closeout

## Status

`v1.6.13-alpha` demotes the v1.6 shadow route from active roadmap work to historical diagnostic work.

## Why

The feature-aware hardened triad27 run in `v1.6.12-alpha` did not earn lane-specific role-stripped false-one discrimination. The route produced useful testing infrastructure, but the scientific claim was not earned.

## Changed

- Updated README current line to `v1.6.13-alpha`.
- Removed shadow result visuals and active shadow data from the README surface.
- Updated ROADMAP current line to `v1.6.13-alpha`.
- Replaced active shadow-roadmap pressure with a historical/HOLD closeout.
- Added `docs/shadow_route_history_and_closeout.md`.
- Updated `docs/version_truth.md`.
- Bumped package version to `1.6.13-alpha` / `1.6.13a0`.

## Boundary

This release does not delete or hide the shadow route. It preserves it as history and records the failed claim boundary.

The native witness remains unchanged:

```text
C_Z = min(D, P, R, B)
```

No role-blind discovery. No detector closeout. No deep81 / wide243 shadow trust.
