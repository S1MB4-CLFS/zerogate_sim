# v1.6.18-alpha — Four Gates debt candidate design

`v1.6.18-alpha` is a logic / math cleanup and design gate. It does not run new heavy evidence and does not start a Zenodo correction route.

## Added

- `docs/four_gates_debt_candidate_design.md`
- `zerogate-four-gates-debt-design`
- `src/zerogate_sim/four_gates_debt_candidate_design_report.py`
- `tests/test_four_gates_debt_candidate_design_report.py`

## Changed

- Public route language now uses **Four Gates of Becoming** instead of temporary acronym framing.
- README / ROADMAP / version truth now treat `v1.6.18-alpha` as the active design gate.
- Native ablation summary status aggregation no longer mixes `no_visible_ablation_wound` into summary rows when a real wound is present elsewhere in the same baseline.

## Scientific boundary

The active target remains controlled synthetic-field evidence:

> Can a final trinary witness distinguish earned-one from raw expression pressure, latent overcrown, relation/return debt, and false-one pressure under controlled synthetic-field adversarial weather?

This version states that relation debt and return debt require **near-success candidates**, not more trap-only adversaries.

## Forbidden claims

- No proof of cosmology.
- No proof of physical dimensional genesis.
- No proof that the universe uses the Four Gates.
- No spacetime metric / field-equation claim.
- No shadow route revival.
- No observed-universe bridge.

Native witness remains:

```text
C_Z = min(D, P, R, B)
```

## Next gate

`v1.6.19-alpha` — debt candidate generator.
