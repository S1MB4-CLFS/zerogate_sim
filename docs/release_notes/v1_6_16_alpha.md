# v1.6.16-alpha — Four-corpus triad27 native evidence

`v1.6.16-alpha` adds the first repaired native evidence gate after the ablation baseline definitions.

## Added

- `zerogate-native-triad27-evidence` console script.
- `src/zerogate_sim/native_triad27_evidence_report.py`.
- `docs/native_triad27_evidence.md`.
- `tests/test_native_triad27_evidence_report.py`.

## Purpose

The gate evaluates completed `triad27` matrix evidence across all four native adversarial families:

- distinction;
- polarity;
- relation;
- return.

It combines native seed-block evidence, native ablation baselines, and trinary lane visibility.

## Boundary

- No Zenodo route yet.
- No shadow revival.
- No observed-universe bridge.
- No native witness mutation.
- Native witness remains `C_Z = min(D, P, R, B)`.

## Next gate

If the native `triad27` evidence earns continuation, the next gate is `v1.6.17-alpha` — native `deep81` / `wide243` evidence.
