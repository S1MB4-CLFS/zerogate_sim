# v1.6.24-alpha — How It Works and closeout route lock

`v1.6.24-alpha` makes the public surface more legible before the final v1.6 audit and closeout gates.

## What changed

- Adds a README `How it works` section after `Why this exists` and before the first visual spine.
- Adds `docs/current_evidence_index.md` as the canonical current evidence map.
- Adds `docs/anti_tautology_audit_plan.md` as the route into `v1.6.25-alpha`.
- Updates ROADMAP to lock the remaining v1.6 sequence:
  - `v1.6.25-alpha` — Anti-Tautology Audit / Role-Dependence Check.
  - `v1.6.26-alpha` — Reproduction Command Package.
  - `v1.6.27-alpha` — Manuscript Correction Package.
  - `v1.6 closeout` — earned / partial / demoted decision before v1.7.
- Updates version truth to `v1.6.24-alpha`.

## Boundary

This version does not add new evidence, mutate native math, revive shadow, start Zenodo work, or claim an observed-universe bridge.

Native witness remains:

```text
C_Z = min(D, P, R, B)
```

## Why this matters

The current evidence is promising, but the next gate must check whether debt states are genuinely witness-derived or too dependent on candidate design. That is the role of `v1.6.25-alpha`.
