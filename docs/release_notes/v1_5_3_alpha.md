# v1.5.3-alpha — Controlled Synthetic-Field Language Boundary

## Purpose

Create the language boundary that lets the active v1.5 experiment line use `controlled synthetic field` while preserving `generated toy field` for the historical first-research-alpha proof record and manuscript lineage.

## Delivered

- Added `docs/controlled_synthetic_field_language.md`.
- Moved runtime-history detail out of README/ROADMAP and into `docs/runtime_ci_support.md`.
- Slimmed README/ROADMAP runtime wording to a one-line boundary plus link.
- Updated active line/version metadata to `v1.5.3-alpha`.
- Added a boundary test that blocks runtime-history bloat and controlled-synthetic-field overclaim.

## Language rule

Allowed:

```text
The v1.5 line reads controlled synthetic-field evidence.
```

Preserved:

```text
The first-research-alpha proof record remains a generated toy-field proof-of-concept.
```

Forbidden:

```text
Controlled synthetic field means physics proof.
```

## Trinary weather reminder

- `triad27 = 3^3`: noise, relation, expansion pressure.
- `deep81 = 3^4`: adds perturbation pressure.
- `wide243 = 3^5`: adds temporal-depth / time-axis pressure.

`wide243` remains scientifically useful for temporal cohesion and full current weather stress, but it is too heavy to run for every small patch.

## Boundary

No engine change. No native gate change. No new proof claim. No role-blind discovery claim. No physical dimensional-genesis claim.
