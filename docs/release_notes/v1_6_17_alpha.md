# v1.6.17-alpha — Deep81 / Wide243 native evidence

`v1.6.17-alpha` adds the repaired native `deep81` / `wide243` evidence gate: deep81 / wide243 native evidence.

It follows the `v1.6.16-alpha` four-corpus `triad27` native evidence result and asks whether the native final trinary witness survives deeper perturbation and temporal-depth stress.

Native witness remains:

```text
C_Z = min(D, P, R, B)
```

## Added

- `zerogate-native-deepwide-evidence` console script.
- `src/zerogate_sim/native_deepwide_evidence_report.py`.
- `docs/native_deepwide_evidence.md`.
- `tests/test_native_deepwide_evidence_report.py`.

## Evidence boundary

This version adds the report tool and route boundary. The actual local `deep81` / `wide243` evidence run is generated separately after CI is green.

The gate requires:

- four adversarial families for `deep81`;
- four adversarial families for `wide243`;
- ablation comparison;
- trinary lane visibility;
- explicit debt-lane reporting for latent overcrown, relation debt, and return debt.

## Forbidden claims

- no Zenodo route yet;
- no observed-universe bridge;
- no shadow route revival;
- no role-blind discovery language;
- no physical dimensional-genesis claim;
- no native witness mutation.

## Next gate if earned

`v1.6.18-alpha` — fresh-seed reproduction and correction package planning.
