# v1.6.21-alpha — Four Gates deep81 / wide243 debt evidence

`v1.6.21-alpha` adds the deeper-weather debt evidence report for the Four Gates of Becoming route.

It scales the `v1.6.20-alpha` triad27 debt evidence into:

```text
deep81  = perturbation / late-shock bridge
wide243 = temporal-depth / time-axis stress
```

## Added

- `zerogate-four-gates-deepwide-debt-evidence` console script.
- `src/zerogate_sim/four_gates_deepwide_debt_evidence_report.py`.
- `docs/four_gates_deepwide_debt_evidence.md`.
- `tests/test_four_gates_deepwide_debt_evidence_report.py`.

## What it evaluates

For each rung, the report requires:

- four native adversary controls: distinction, polarity, relation, return;
- one `four_gates_debt` candidate matrix;
- native ablation comparison;
- trinary state-lane visibility;
- relation debt and return debt visibility.

Native witness remains:

```text
C_Z = min(D, P, R, B)
```

## Boundary

No Zenodo route, no shadow revival, no observed-universe bridge, no spacetime metric claim, and no native witness mutation.

If both deeper rungs expand, the next gate is `v1.6.22-alpha`: fresh-seed debt reproduction. If one rung becomes partial, inspect before moving forward.
