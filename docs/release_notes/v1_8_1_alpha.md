# v1.8.1-alpha — Lineage-Bearing Predictor Package (Prior-Touch Semantics)

This development checkpoint replaces one-snapshot lineage language with an
exact early/witness/late observable path and names its implemented semantics
precisely: prior-touch support, not continuous persistence.

## Added

- exact three-frame schema derived from and hash-bound to the v1.8.0
  seven-field observable schema;
- threshold-free prior-touch score and no-prior-touch ablation;
- explicit dormant-reappearance behavior and formula-input canaries;
- predictor execution loaded from the verified source-byte snapshot;
- allowed-root source manifest with input, purpose, and declaration hashes;
- strict post-freeze recomputation and receipt verification;
- complete v1.8.2 development-method lock: threshold boundaries, four generator
  lineages, nested lineage folds, lexicographic selection, baselines, dual-mode
  ablations, uncertainty, duplicate policy, permutation rules, and stop states;
- exact baseline formulas, metric denominators, boundary-margin calculation,
  zero-tolerance model comparison, failure fixtures, and deterministic
  SHA-256-indexed cluster-bootstrap interval construction;
- workflow-research ledger for end-to-end Codex economics through v1.8.4.

## Adversarial repairs

- closed the gap between hashed disk source and executed mutable imports;
- replaced unverified holdout-history assertions with scoped source
  declarations and an explicit declarations-are-not-proof boundary;
- narrowed scientific wording from continuous lineage to prior-touch support;
- added per-input formula canaries and dormant-reappearance coverage;
- made failed-write cleanup remove only files owned by the current writer;
- added a strict consumer for frozen score, manifest, receipt, source, and
  package verification.

## Decision boundary

`LOCAL_GREEN_LINEAGE_PACKAGE_ONLY` means the schema, formula canaries,
source/configuration binding, freeze, and verification path work locally. It is
not an empirical discrimination result. Scientific authority remains
v1.7.11 `0 / HOLD`; the selected threshold is null and no holdout is opened in
this version.

The user has authorized full-agent implementation, commit/push/PR/CI/merge,
scientific threshold selection, and holdout execution through v1.8.4. Tags,
releases, DTA transfer, manuscript prose, Zenodo, and email remain forbidden.
