# v1.5.4-alpha — Wide243 Historical Evidence Intake

## Purpose

Record the uploaded original and fresh-seed `wide243` proof archives as historical evidence intake before fresh heavy controlled reruns are attempted.

## Delivered

- `docs/reports/wide243_historical_evidence_intake.md`;
- README / ROADMAP pointers for the evidence-intake boundary;
- `tests/test_wide243_historical_evidence_intake.py`;
- version bump to `1.5.4-alpha`.

## Evidence posture

The intake preserves the first-research-alpha proof floor:

- `13,122` seeded runs;
- `22,131` final earned-one events;
- `2,388` raw false-one pressures detected and demoted;
- `2,442` latent overcrown pressures held in zero;
- `0` final false-one crowns;
- `0` trap final crowns.

## Scientific boundary

The uploaded `wide243` archives remain historical three-corpus proof records. They evaluate return internally through the native `C_Z = min(D, P, R, B)` gate law, but they are not the same as the newer four-gate adversary preset line with dedicated return-adversary coverage.

This version does not change the engine, rerun heavy matrices, solve role-blind detection, or claim physical dimensional genesis.

## Next evidence order

1. Preserve the historical `wide243` proof floor.
2. Run a non-smoke threshold sweep.
3. Run fresh controlled `deep81` four-gate evidence.
4. Run fresh controlled `wide243` four-gate evidence when disk/time are deliberately allocated.
5. Use those outputs to design the role-blind shadow reader.
