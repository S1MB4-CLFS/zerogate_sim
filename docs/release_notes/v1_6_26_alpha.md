# v1.6.26-alpha — Reproduction Command Package

`v1.6.26-alpha` adds the Four Gates reproduction command package.

It provides a clean path for a skeptical reader to:

- run a small triad27 reproduction smoke;
- understand the full reference/fresh seed reproduction route;
- locate canonical current evidence folders;
- inspect expected outputs and pass/fail boundaries.

It does not add new science, does not change the native witness, does not start Zenodo correction, does not revive shadow, and does not claim observed-universe proof.

Native witness remains:

```text
C_Z = min(D, P, R, B)
```

The anti-tautology boundary remains active:

```text
debt states are designed-profile shaped but witness-counted and reproducible;
not independent role-blind discovery.
```

Next gate:

```text
v1.6.27-alpha — Manuscript Correction Package
```
