# v1.7.5-alpha — Masked Role-Dependence Audit

`v1.7.5-alpha` locks the masked role-dependence audit required before fresh holdout pressure.

It asks whether the v1.7 lane outputs remain witness-computed when direct designed role labels are masked.

## What changed

- Adds `docs/v1_7_masked_role_dependence_audit.md`.
- Adds `docs/v1_7_masked_numeric_visibility.md`.
- Adds `docs/v1_7_role_dependence_pressure.md`.
- Adds `docs/v1_7_role_blind_forbidden_language.md`.
- Adds `zerogate-v1-7-masked-role-audit`.
- Adds `tests/test_v1_7_masked_role_dependence_audit.py`.

## Boundary

This gate does not close the core question. It adds no new heavy evidence crown, does not start manuscript v2, does not revive shadow as discovery, does not upload to Zenodo, and does not claim role-blind false-one detection.

Passing this gate may support only this bounded statement:

> Under masked numeric review, controlled designed-profile outputs can remain witness-computed rather than direct role-label recounting.

It may not support this stronger statement:

> The witness independently discovers all false-one or debt states in unlabeled fields.

## Native witness

```text
C_Z = min(D, P, R, B)
```

Unchanged.

## Next gate

`v1.7.6-alpha — Fresh Holdout Synthetic-Field Challenge`.
