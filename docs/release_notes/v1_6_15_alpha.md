# v1.6.15-alpha — Native ablation baselines

`v1.6.15-alpha` adds the native ablation baselines enemy layer required before the repaired native proof route can advance to four-corpus `triad27` evidence.

## Added

- `zerogate-native-ablation-baselines`
- `src/zerogate_sim/native_ablation_baselines_report.py`
- `docs/native_ablation_baselines.md`
- `tests/test_native_ablation_baselines_report.py`

## Scientific purpose

The gate prevents `0 final false-one crowns` from becoming an empty victory. A dead-safe witness can crown nothing. The native final trinary witness must instead preserve earned-one, hold structured zero pressure, and demote false-one pressure better than raw, binary, and ablated alternatives.

## Baseline enemies

The baseline set includes raw-expression-only, binary raw/fail, dead-safe no-crown, no-zero-hold, no-false-one-demotion, no-echo-independence, no-return-debt witness, no-relation-gate raw, no-return-gate raw, and average-gate raw alternatives.

## Boundary

Native witness remains:

```text
C_Z = min(D, P, R, B)
```

No Zenodo route yet. No shadow revival. No observed-universe bridge claim. No native witness mutation.

Next gate: `v1.6.16-alpha` four-corpus `triad27` native evidence.
