# v1.7.9-alpha — Reviewer Start Here / Reproduction Package

`v1.7.9-alpha` creates the reviewer package and narrow reviewer door after repo cleanup and before core-question closeout.

## Why this exists

The v1.7 line now has a core question contract, return trace, lane taxonomy, falsifier matrix, perturbation spectrum, masked role-dependence audit, fresh holdout ladder, anti-tautology audit, and front-page cohesion. A reviewer should not need private chat context to know where to start or what to run.

## Main changes

- Current public line moves to `v1.7.9-alpha`.
- Adds top-level `REVIEWER_START_HERE.md`.
- Adds `docs/v1_7_minimal_reproduction.md`.
- Adds `docs/v1_7_expected_outputs.md`.
- Adds `docs/v1_7_claim_boundary_card.md`.
- Adds `docs/v1_7_evidence_manifest.md` and `docs/v1_7_reviewer_start_here.md`.
- Adds `scripts/run_v1_7_small_reproduction.ps1`.
- Adds `zerogate-v1-7-reviewer-package` CLI.
- Keeps the heavy weather ladder separated: triad27 -> inspect -> deep81 -> inspect -> wide243 -> inspect.

## Boundary

This version preserves:

```text
C_Z = min(D, P, R, B)
```

It does not close the core question, start manuscript v2, upload to Zenodo, claim role-blind discovery, claim independent generator validation, or claim physics/cosmology/observed-universe proof.

## Next gate

`v1.7.10-alpha — Core Question Closeout`.
