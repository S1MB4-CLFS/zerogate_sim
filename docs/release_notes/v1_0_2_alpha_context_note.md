# v1.0.2-alpha Context Note

This context note belongs with the first public release if the release has not yet been published.

Before publishing the GitHub release, the repository should include:

- the original pre-simulation `Zero-Gate Dimensional Genesis` manuscript as historical source material;
- a reader-facing DREED / `active_inference_review_lens_v0` method note;
- the full assistant-side DREED protocol for traceability;
- a clear statement that the original manuscript is not the final simulation-supported paper.

The release should not attach large generated run folders or proof bundles to GitHub. Evidence archives can later be placed in Zenodo or in a dedicated evidence release.
