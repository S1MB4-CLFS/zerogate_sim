# Native Four-Corpus Triad27 Evidence

**Version:** `v1.6.16-alpha`  
**Status:** native controlled synthetic-field evidence gate  
**Native witness:** `C_Z = min(D, P, R, B)`

`v1.6.16-alpha` moves the repaired native route from definitions into the first local weather evidence gate.

It asks:

> Can a final trinary witness distinguish earned-one from raw expression pressure, latent overcrown, relation/return debt, and false-one pressure under controlled synthetic-field adversarial weather?

## What is tested

The gate requires completed `triad27` matrices for four adversarial families:

- distinction theater;
- polarity theater;
- relation echo / borrowed coherence;
- return theater / shallow return pressure.

The report rejects missing gates and non-`triad27` inputs. The point is not bigger weather yet. The point is whether the native witness can stand at the local-expression rung before it is allowed to spend trust on `deep81` or `wide243`.

## What the report writes

```text
native_triad27_evidence_read.md
native_triad27_evidence_decision.json
native_triad27_seed_block_summary.csv
native_triad27_ablation_summary.csv
native_triad27_ablation_gate_summary.csv
native_triad27_ablation_definitions.csv
native_triad27_state_lanes.csv
native_triad27_evidence_audit.json
native_triad27_evidence_bundle.zip
```

The report combines:

- native four-gate seed-block counts;
- native ablation baseline results;
- trinary lane visibility for `+1 / 0 / -1`;
- decision language that refuses to treat zero false crowns as enough by itself.

## Pass posture

A strong result must show all three movements:

```text
+1 earned-one preserved
 0 latent / relation / return pressure held as structured zero
-1 false-one pressure demoted without final false crowns
```

It must also show that ablations fail in meaningful ways:

- dead-safe no-crown loses earned-one;
- raw-expression-only overcrowns false pressure;
- no-zero-hold promotes structured zero pressure;
- no-relation / no-return / average-gate variants expose why weakest-gate `C_Z` matters.

## Boundary

This is not Zenodo correction, not observed-universe bridge work, not shadow revival, and not proof of physical dimensional genesis.

A clean `triad27` result only earns permission to continue to the next native weather rung.
