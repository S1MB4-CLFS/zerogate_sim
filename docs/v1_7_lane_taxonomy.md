# v1.7.2-alpha — Lane Taxonomy and Latent Overcrown Repair

**Version:** `v1.7.2-alpha`  
**Status:** lane taxonomy locked; latent overcrown repaired as explicit HOLD  
**Native witness:** `C_Z = min(D, P, R, B)`

## Core question

> Can a final trinary witness distinguish earned-one from raw expression pressure, latent overcrown, relation debt, return debt, and false-one pressure under controlled synthetic-field adversarial weather?

`v1.7.2-alpha` does not answer the full question and does not add a new heavy evidence crown. It makes the lane set inspectable before the baseline and ablation falsifier matrix begins.

## Required lanes

| lane | state | current interpretation |
|---|---|---|
| `earned_one` | `+1` | final expression; crown accepted only after raw pressure survives witness stack |
| `raw_expression_pressure` | pre-final pressure | local +1-style pressure, not final truth |
| `latent_overcrown` | `0` structured zero / fragile HOLD | latent/probe pressure held in zero; historical support exists, but current Four Gates fresh-seed evidence made the lane seed-sensitive |
| `relation_debt` | `0` structured zero | meaningful but incomplete, borrowed, under-owned, unstable, or global-only relation |
| `return_debt` | `0` structured zero | `Gamma = D * P * R` is meaningful but observed `B`, memory, closure, or return-depth is incomplete |
| `false_one_pressure` | `-1` resist / demotion | trap or unsafe pressure exposed before final crown |

## Repair made here

The repair is not to delete latent overcrown and not to pretend it is reproduced. The repair is to make its status impossible to smuggle:

```text
latent overcrown = named lane + historical proof-record support + current seed-sensitivity
v1.7.2 status = HOLD until reproduced, narrowed, or closed partial
```

This means the final `v1.7` closeout cannot use latent overcrown as a clean reproduced lane unless later evidence earns that.

## What stays separate

- raw expression pressure is not earned-one;
- latent overcrown is not false-one pressure;
- relation debt is not return debt;
- return debt is not zero-crossing theater;
- false-one pressure is not a softened latent hold;
- zero is not a flat non-crown bucket.

## Gate into v1.7.3

`v1.7.3-alpha` may begin because the lane set now names what the baselines must fail to explain.

Allowed next question:

> Do raw, binary, dead-safe, average-gate, no-return, no-relation, no-lineage, no-echo, or no-zero-hold witnesses explain the same lane pattern?

Forbidden next move:

> Treat this taxonomy as evidence that the full core question is already answered.
