# v1.7.5-alpha — Masked Numeric Visibility

**Version:** `v1.7.5-alpha`  
**Native witness:** `C_Z = min(D, P, R, B)`  
**Core question:** can a final trinary witness distinguish earned-one from raw expression pressure, latent overcrown, relation debt, return debt, and false-one pressure under controlled synthetic-field adversarial weather?


## Visibility rule

The audit must show what remains visible when direct designed role labels are hidden.

Required numeric surfaces:

| signal | masked visibility requirement | safe output |
|---|---|---|
| earned-one | final `+1` remains visible without direct role labels | crown only when return-depth, lineage, and independence are paid |
| raw expression pressure | raw local pressure remains separate from final `+1` | pressure, not truth |
| relation debt | borrowed or unstable relation remains structured zero | `0` relation-debt hold |
| return debt | `Gamma = D * P * R` without observed return remains debt | `0` return-debt hold |
| false-one pressure | adversarial pressure remains visible enough to demote | `-1` demotion or `0` quarantine |
| final false-one crowns | stop-condition count remains explicit | must stay `0` |

Latent overcrown remains a required but fragile lane. If masked review cannot reproduce it later, the v1.7 closeout must narrow or close at `0` rather than pretend the lane was fully answered.
