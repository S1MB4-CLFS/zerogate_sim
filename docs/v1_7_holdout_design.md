# v1.7.6-alpha — Fresh Holdout Design

**Version:** `v1.7.6-alpha`  
**Native witness:** `C_Z = min(D, P, R, B)`  
**Gate:** Fresh Holdout Synthetic-Field Challenge

`v1.7.6-alpha` locks the holdout design required before the core question can be closed. It does not answer the core question by itself. It defines what a fresh controlled synthetic-field challenge must contain so the witness cannot merely repeat the known reference profile.

## Required holdout pressure

```text
fresh seeds not used in the reference run
held-out profile variants
controlled weather shifts
candidate-name masking
lane-level expected-output manifest written before run
```

The holdout remains controlled synthetic-field work. It is not role-blind discovery, not an observed-universe bridge, and not external empirical validation.

## 27 / 81 / 243 policy

The three weather rungs are all required for the eventual full holdout answer:

```text
triad27 = 3^3 local expression holdout smoke
deep81 = 3^4 perturbation / late-shock holdout bridge
wide243 = 3^5 temporal-depth / full controlled weather stress
```

`v1.7.6-alpha` locks and can evaluate the holdout contract. It does not require CI to run the full heavy 27/81/243 evidence stack. Once `v1.7.6-alpha` is CI green, run the full holdout ladder before `v1.7.7-alpha` audits anti-tautology and role-dependence pressure. `v1.7.8-alpha` packages the reviewer/reproduction path, and `v1.7.9-alpha` closes the core question using whatever evidence has actually been run and passed.

## Pass condition

The same qualitative lane pattern survives fresh holdout pressure:

```text
earned-one preserved
raw expression remains pressure
latent overcrown held when present
relation debt visible
return debt visible
false-one pressure demoted
final false-one crowns = 0
```

## Stop condition

```text
any final false-one crown appears
expected-output manifest is written after the result
candidate names reveal the expected lane
result only survives the known reference profile
native witness is changed to rescue the outcome
```

No crown without return. No holdout without prewritten expectation. No goblin victory lap because it guessed the answer after peeking.
