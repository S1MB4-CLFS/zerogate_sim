# Proof Records

This folder contains small, source-control-safe summaries of the v1.0-alpha first-research-alpha proof record.

The key posture:

- `13,122` seeded toy-field runs;
- `22,131` final earned-one events;
- `2,388` raw false-one pressures detected and demoted;
- `0` final false-one crowns.

The full generated proof weather should stay outside Git by default. Keep large bundles in release assets or Zenodo evidence/data records.
