# v1.7.6-alpha — Holdout Weather Ladder

**Version:** `v1.7.6-alpha`  
**Native witness:** `C_Z = min(D, P, R, B)`

`v1.7.6-alpha` locks the three-rung holdout ladder. The ladder is still controlled synthetic-field adversarial weather. It is not role-blind discovery, not external validation, and not an observed-universe bridge.

## Required ladder

```text
triad27 = 3^3 local expression weather
deep81  = 3^4 perturbation / late-shock bridge
wide243 = 3^5 temporal-depth / time-axis stress
```

## Run timing

```text
v1.7.6-alpha CI green
-> triad27 holdout summary
-> deep81 holdout summary
-> wide243 holdout summary
-> v1.7.7-alpha reviewer package
-> v1.7.8-alpha core question closeout
```

The full ladder should run before `v1.7.7-alpha` packages the reviewer path. `v1.7.7-alpha` should package actual evidence summaries and commands, not a promise to maybe test later. CI should not carry the heavy evidence burden; CI guards the contract, schema, and evaluator.

## Tier meanings

| rung | cells | role |
|---|---:|---|
| `triad27` | 27 | small local expression weather; first fresh holdout smoke lane |
| `deep81` | 81 | perturbation / late-shock bridge; medium holdout pressure |
| `wide243` | 243 | temporal-depth / time-axis stress; full controlled synthetic-field weather |

A smaller-tier pass can justify the next tier. It cannot replace the next tier.
