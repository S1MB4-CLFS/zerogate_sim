# Manuscript Correction Package

**Introduced:** `v1.6.27-alpha`  
**Current closeout note:** `v1.6.28-alpha` records the v1.6 closeout after this Manuscript Correction Package.  
**Native witness:** `C_Z = min(D, P, R, B)`  
**Status:** planning package only; not the full v2 manuscript.

This gate prepares the material needed to later write a corrected manuscript version without starting the Zenodo route yet.

The current Zenodo manuscript remains a strong historical first-research-alpha artifact. It should be preserved as the original paper, not erased or quietly rewritten.

## Why this package exists

The original paper correctly preserved the core repair:

```text
Raw expression is pressure. Earned-one is final +1.
```

The newer repo evidence adds a second repair:

```text
Structured zero is not just non-crown. It can hold relation debt and return debt.
```

A later v2 manuscript should therefore be additive and corrective:

- keep the original conceptual spine;
- preserve the first-alpha proof record as history;
- explain the three-corpus / four-gate distinction honestly;
- add repaired Four Gates debt evidence only if v1.7 earns the operational package;
- keep all physics, topology, and observed-universe material in analogy/future-work lanes.

## Package output

Generate the package with:

```powershell
python -m zerogate_sim.four_gates_manuscript_correction_package_report `
  --out runs\four_gates_manuscript_correction_package_v1_6_27
```

It writes:

```text
four_gates_manuscript_correction_package_read.md
four_gates_manuscript_correction_package_decision.json
four_gates_v2_manuscript_outline.md
four_gates_manuscript_patch_map.csv
four_gates_claim_lane_register.csv
four_gates_canonical_evidence_table.csv
four_gates_zenodo_new_version_plan.md
four_gates_manuscript_correction_package_audit.json
four_gates_manuscript_correction_package_bundle.zip
```

## Current allowed claim lane

The candidate sentence for later v1.7/v2 work is:

> In controlled synthetic adversarial fields, the Four Gates witness operationalizes a synthetic zero-zone gating principle: it delays premature expression, preserves earned expression, holds unresolved relation/return debt as structured zero, and demotes false-one pressure better than raw, binary, dead-safe, and ablated witnesses.

This is not yet the final paper claim. It remains a candidate until closeout and v1.7 packaging.

## Boundaries

This package does not:

- create a full v2 paper;
- upload to Zenodo;
- claim physics proof;
- claim cosmology proof;
- claim role-blind discovery;
- revive the shadow route;
- change native witness math.

The old manuscript remains historical. The future manuscript can make it honest at the next level only after the repo earns the route.
