# v1.7.5-alpha — Role-Dependence Pressure

**Version:** `v1.7.5-alpha`  
**Native witness:** `C_Z = min(D, P, R, B)`  
**Core question:** can a final trinary witness distinguish earned-one from raw expression pressure, latent overcrown, relation debt, return debt, and false-one pressure under controlled synthetic-field adversarial weather?


## Pressure states

| state | meaning | action |
|---|---|---|
| masked core lanes visible | numeric witness pattern separates earned-one, relation debt, return debt, false-one pressure, and zero false crowns | expand inside controlled designed scenario only |
| designed-profile shaped hold | outputs are witness-computed but still depend on designed profile family | witness / hold; later holdout required |
| role leakage pressure | direct role information or role-like proxy carries lane assignment | hold or resist until leakage is removed or bounded |
| label-only lane failure | lane assignment fails when role labels are hidden | resist; not witness-computed yet |
| false crown stop | a final false-one crown appears | resist and block closeout |

## Clean sentence

Passing this gate says the current controlled designed-profile evidence is not merely role-label recounting.

It does not say the system has solved unknown-field discovery.
