# v1.7.4-alpha — Weather Curve Summary

**Version:** `v1.7.4-alpha`  
**Native witness:** `C_Z = min(D, P, R, B)`

The v1.7 perturbation spectrum is organized by the established weather ladder:

| weather view | shape | purpose |
|---|---|---|
| `triad27` | `3^3` | local expression weather; confirms lane visibility before heavier pressure |
| `deep81` | `3^4` | perturbation / late-shock bridge; asks whether raw pressure moves without unsafe final crowns |
| `wide243` | `3^5` | temporal-depth / time-axis stress; asks whether return-depth, lineage, echo, debt, and demotion stay visible |

## Required witness posture

Across `triad27`, `deep81`, and `wide243`, the repo must be able to report:

```text
earned-one
raw expression pressure
latent overcrown
relation debt
return debt
echo dependence
lineage instability
false-one pressure
final false-one crowns
```

The weather ladder is not a scoreboard. It is a pressure map.

## Boundary

This file is a v1.7.4 witness contract, not a new proof record. Full answer pressure waits for later gates and closeout.
