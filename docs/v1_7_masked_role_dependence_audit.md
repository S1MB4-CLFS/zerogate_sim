# v1.7.5-alpha — Masked Role-Dependence Audit

**Version:** `v1.7.5-alpha`  
**Native witness:** `C_Z = min(D, P, R, B)`  
**Core question:** can a final trinary witness distinguish earned-one from raw expression pressure, latent overcrown, relation debt, return debt, and false-one pressure under controlled synthetic-field adversarial weather?


## Purpose

The current controlled harness may use designed roles. That is allowed. `v1.7.5-alpha` asks whether the reported lanes are still computed by the witness stack when direct role labels are masked.

The audit target is not role-blind discovery. The target is narrower:

> Separate witness-computed lane visibility from role-label bookkeeping.

## Required masked lanes

```text
earned-one
raw expression pressure
latent overcrown
relation debt
return debt
false-one pressure
final false-one crowns
```

## Pass / hold / resist grammar

```text
+1 expand — masked numeric review keeps core lanes visible and final false-one crowns remain zero, but no role-blind discovery is claimed.
 0 witness/hold — outputs remain useful but designed-profile shaping, leakage pressure, or partial visibility still bounds the claim.
-1 resist — lane assignment requires role labels, direct leakage carries the result, or any final false-one crown appears.
```

## Required separation

Designed role labels may define the adversarial harness. They may not be the hidden mechanism that assigns lanes. A lane should have a witness-computed surface: return-depth, relation/return debt, echo/lineage diagnostics, final trinary state, and false-crown safety.

## Forbidden translation

Do not translate masked numeric visibility into role-blind discovery. Role-blind language waits for later role-stripped, holdout, and independent generator evidence.

## Next gate

`v1.7.6-alpha — Fresh Holdout Synthetic-Field Challenge`.
