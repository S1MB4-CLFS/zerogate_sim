# v1.7.4-alpha — Perturbation Spectrum Witness

**Version:** `v1.7.4-alpha`  
**Native witness:** `C_Z = min(D, P, R, B)`  
**Status:** perturbation spectrum witness locked; core question not closed

## Core question

> Can a final trinary witness distinguish earned-one from raw expression pressure, latent overcrown, relation debt, return debt, and false-one pressure under controlled synthetic-field adversarial weather?

`v1.7.4-alpha` does not answer the question yet. It locks the spectrum under perturbation required before any later closeout can honestly say the witness fails safely.

## Required spectrum lanes

## Spectrum principle

The useful pressure view is not one scalar and not one victory count. The witness must show a lane spectrum:

```text
earned-one
raw expression pressure
latent overcrown
relation debt
return debt
echo dependence
lineage instability
false-one pressure
final false-one crowns
```

The central requirement:

```text
raw pressure may move, debt pressure may rise, false-one pressure may surface,
but final witness does not crown unsafe pressure.
```

## Required lane behavior

| lane | surface | required behavior |
|---|---|---|
| earned-one | `+1` | remains visible where structure survives; collapses safely when structure is destroyed |
| raw expression pressure | pre-final pressure | may rise or destabilize under weather; never becomes final truth by volume alone |
| latent overcrown | `0` | remains explicit HOLD until reproduced, narrowed, or closed partial |
| relation debt | `0` | holds borrowed / under-owned relation pressure as structured zero |
| return debt | `0` | holds `Gamma`-high / `B`-incomplete pressure as structured zero |
| echo dependence | `0` or `-1` pressure | blocks borrowed field coherence from becoming earned-one |
| lineage instability | `0` or `-1` pressure | blocks one-frame expression from becoming final truth |
| false-one pressure | `-1` pressure / demotion | remains visible but uncrowned |
| final false-one crowns | breach counter | remains zero for any later `+1` closeout candidate |

## Boundary

This is still controlled synthetic-field software-theory work. It does not prove physics, cosmology, observed reality, role-blind discovery, or external dimensional claims.
