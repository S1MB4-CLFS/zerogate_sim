# v1.7 Core Question Contract

**Introduced:** `v1.7.0-alpha`  
**Status:** first coded v1.7 gate / no new evidence claim  
**Native witness:** `C_Z = min(D, P, R, B)`

## Core question

> Can a final trinary witness distinguish earned-one from raw expression pressure, latent overcrown, relation debt, return debt, and false-one pressure under controlled synthetic-field adversarial weather?

This is the main question for the current software line.

`v1.7.0-alpha` opens the answer line. It does not close it.

## Claim under test

> Inside controlled synthetic-field adversarial weather, the final trinary witness distinguishes earned-one from raw expression pressure, latent overcrown, relation debt, return debt, and false-one pressure better than raw, binary, dead-safe, average-gate, and ablated witnesses, while preserving final false-one crowns at zero.

This is a controlled synthetic-field claim. It is not a physics, cosmology, observed-universe, or role-blind discovery claim.

## Decision state

```text
0 — question contract opened; no new evidence crown.
```

The contract is successful only if roadmap, README, and claim docs carry the same bounded question and every strong word has a falsifier.

## Required lane set

| lane | witness state | why it matters |
|---|---|---|
| earned-one | `+1` | final expression must still be preserved; a dead-safe witness is not enough |
| raw expression pressure | pre-final pressure | local expression must not be mistaken for final truth |
| latent overcrown | `0` | seed-sensitive pressure must be held or demoted honestly, not hidden |
| relation debt | `0` | relation-like pressure must be held when it is incomplete or borrowed |
| return debt | `0` | return-potential must not be confused with observed return-depth |
| false-one pressure | `-1` | trap pressure must be demoted before final crown |

## Closeout rule

The core question can close only as:

```text
+1 — full controlled synthetic-field answer earned.
 0 — partial / bounded / unresolved; preserve evidence but do not crown the full question.
-1 — no; the full claim is not earned under controlled synthetic-field adversarial weather.
```

## Next gate

`v1.7.1-alpha` must lock the return-gate trace before the roadmap adds heavier evidence work.
