# Native Deep81 / Wide243 Evidence

**Version:** `v1.6.17-alpha`  
**Status:** controlled synthetic-field native evidence gate — deep81 / wide243 native evidence  
**Boundary:** no Zenodo route, no observed-universe bridge, no shadow revival, no native witness mutation

`v1.6.17-alpha` adds the next repaired native evidence gate after the `v1.6.16-alpha` four-corpus `triad27` result.

It does not treat bigger weather as victory. It asks whether the native final trinary witness survives deeper perturbation and temporal-depth stress while keeping the core claim honest.

Native witness remains:

```text
C_Z = min(D, P, R, B)
```

## Weather rungs

```text
triad27 = 3^3 local expression weather
deep81  = 3^4 perturbation / late-shock bridge
wide243 = 3^5 temporal-depth / time-axis stress
```

`v1.6.17-alpha` evaluates `deep81` and `wide243` together after the repaired `triad27` gate. It requires all four adversarial families for each rung:

- distinction;
- polarity;
- relation;
- return.

## Core question

> Can a final trinary witness distinguish earned-one from raw expression pressure, latent overcrown, relation/return debt, and false-one pressure under controlled synthetic-field adversarial weather?

## Debt-lane requirement

This gate explicitly reports whether deeper weather surfaces:

- latent overcrown;
- relation debt;
- return debt;
- false-one pressure;
- final false-one crown breaches, if any.

If `deep81` / `wide243` preserve earned-one and demote false-one pressure but still show no relation-debt or return-debt signal, the native route may be strong for false-crown refusal while the full structured-zero debt grammar remains partial.

## Report tool

```powershell
& $P -m zerogate_sim.native_deepwide_evidence_report `
  --deep81-matrix-dir runs\native_deepwide_v1_6_17\matrix\deep81\distinction_deep81 `
  --deep81-matrix-dir runs\native_deepwide_v1_6_17\matrix\deep81\polarity_deep81 `
  --deep81-matrix-dir runs\native_deepwide_v1_6_17\matrix\deep81\relation_deep81 `
  --deep81-matrix-dir runs\native_deepwide_v1_6_17\matrix\deep81\return_deep81 `
  --wide243-matrix-dir runs\native_deepwide_v1_6_17\matrix\wide243\distinction_wide243 `
  --wide243-matrix-dir runs\native_deepwide_v1_6_17\matrix\wide243\polarity_wide243 `
  --wide243-matrix-dir runs\native_deepwide_v1_6_17\matrix\wide243\relation_wide243 `
  --wide243-matrix-dir runs\native_deepwide_v1_6_17\matrix\wide243\return_wide243 `
  --out runs\native_deepwide_v1_6_17\native_deepwide_evidence
```

The report writes:

- `native_deepwide_evidence_read.md`;
- `native_deepwide_evidence_decision.json`;
- `native_deepwide_rung_summary.csv`;
- `native_deepwide_seed_block_summary.csv`;
- `native_deepwide_ablation_summary.csv`;
- `native_deepwide_ablation_gate_summary.csv`;
- `native_deepwide_state_lanes.csv`;
- `native_deepwide_evidence_bundle.zip`.

## Pass shape

A clean result needs more than `0` final false-one crowns.

It must show:

- earned-one preservation;
- raw expression pressure separated from final earned-one;
- false-one pressure demoted;
- ablation enemies wounded;
- debt-lane visibility reported honestly;
- no native witness mutation.

## Boundary

This is still controlled synthetic-field software evidence. It does not prove physical dimensional genesis, cosmology, or that the observed universe uses ZeroGateSim.

If this gate earns expansion, the next allowed route is `v1.6.18-alpha`: fresh-seed reproduction and correction package planning. Zenodo remains deferred until the correction package is coherent.
