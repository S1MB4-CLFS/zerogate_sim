# Four Gates Deep81 / Wide243 Debt Evidence

**Version:** `v1.6.21-alpha`  
**Status:** deeper-weather evidence gate for the Four Gates of Becoming debt route

`v1.6.21-alpha` scales the repaired `triad27` debt result into deeper controlled synthetic weather:

```text
deep81  = 3^4 perturbation / late-shock bridge
wide243 = 3^5 temporal-depth / time-axis stress
```

This gate is allowed only because `v1.6.20-alpha` made relation debt and return debt visible at `triad27` scale.

## Native witness

```text
C_Z = min(D, P, R, B)
```

This version does not mutate the native witness. It evaluates whether the Four Gates debt pattern survives deeper pressure.

## Required inputs

Each weather rung requires all four native adversary controls:

- distinction;
- polarity;
- relation;
- return.

Each weather rung also requires one `four_gates_debt` matrix:

- `deep81` + `four_gates_debt`;
- `wide243` + `four_gates_debt`.

## Required evidence lanes

A strong result requires all of the following:

- earned-one remains visible;
- native false-one pressure is demoted;
- final false-one crowns remain `0`;
- latent overcrown remains visible or explicitly reported;
- relation debt remains visible;
- return debt remains visible;
- ablation enemies are wounded;
- no shadow route or observed-universe bridge claim is revived.

## Decision language

`expand_four_gates_deepwide_debt_evidence` means both `deep81` and `wide243` preserve the debt pattern under deeper pressure.

`witness_four_gates_deepwide_debt_partial` means the core witness remains useful, but one or more debt lanes weaken and must be inspected before fresh-seed reproduction.

`resist_four_gates_deepwide_debt_breach_or_regression` means a breach or regression appeared and the evidence route must stop.

## Boundary

This is still controlled synthetic-field software evidence. It is not Zenodo correction, not physical dimensional proof, not observed-universe bridge, not shadow route revival, and not a spacetime metric claim.


Keyword boundary: deep81 / wide243 debt evidence.
