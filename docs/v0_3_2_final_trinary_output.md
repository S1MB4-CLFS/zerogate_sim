# v0.3.2-alpha — Final Trinary Output

Status: witness repair, no four-gate mutation.

v0.3.2 makes earned-one the primary final output.

The earlier stack correctly found that raw expression is not final truth. A raw +1 can be a true expresser, a latent overcrown, or a false-one breach. The final output therefore reads raw expression through truth role and echo-independence before assigning the final trinary crown.

Core sentence:

> Raw expression is pressure. Earned-one is final +1.

Final bands:

- `+1 earned_one`: expression survived truth-role and echo-independence witness.
- `0+ latent_overcrown_demoted`: latent/probe expressed locally, but remains zero-held.
- `0 relation_debt_hold`: expression depends on support weather and needs retest.
- `0 latent_contained` / `0 expresser_wound`: held without final crown.
- `-1 false_one_demoted`: trap expressed locally, but final output refuses the crown.
- `-1 trap_contained`: trap remained contained.

New outputs:

- `matrix_final_output_summary.csv`
- `matrix_final_output_read.md`
- `matrix_theory_confirmation_read.md`

This version also adds `python -m zerogate_sim.finalize`, which can create the final output files from an existing matrix directory that already contains `matrix_earned_one_summary.csv`. That avoids rerunning a long matrix just to generate the final witness files.

Claim boundary:

`toy_domain_confirmed` means the current software theory is internally confirmed inside structured toy fields: earned-one separates real expressers from false-one pressure and latent overcrown. It does not mean proof of cosmology, physics, or final trinary mathematics.
