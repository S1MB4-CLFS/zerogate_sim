# Native Four-Gate Claim Audit

**Version:** `v1.6.14-alpha`  
**Status:** route correction / scientific readiness audit  
**Boundary:** not Zenodo yet, not observed-universe bridge, not shadow revival, not physics proof

## Core question

> Can a final trinary witness distinguish earned-one from raw expression pressure, latent overcrown, relation/return debt, and false-one pressure under controlled synthetic-field adversarial weather?

This is the native question ZeroGateSim must answer before manuscript correction or external-review packaging.

## Why this audit exists

The historical first-alpha result and later controlled evidence showed useful native witness behavior, but the next claim must be sharper than `0` final false-one crowns.

A witness that refuses everything can also avoid false crowns. That is not intelligence. That is a locked door with a PhD.

The native claim must show three movements together:

```text
+1 earned-one preserved
 0 structured zero hold: latent overcrown, relation debt, return debt, quarantine / not-yet
-1 false-one pressure demoted
```

The native witness remains:

```text
C_Z = min(D, P, R, B)
```

No new gate is introduced here.

## Four gates

- **Distinction** — something becomes separable from background.
- **Polarity** — distinction gains meaningful positive and negative expression around zero.
- **Relation** — polarity becomes bound into stable relation rather than split or drift.
- **Return** — expressed structure folds back toward zero while preserving coherence.

The repaired route requires all four as dedicated adversarial families before broader claims are trusted.

## Claim lanes

| lane | what must be shown | bad shortcut |
|---|---|---|
| `+1 earned-one` | the witness preserves earned expression | avoid false crowns by crowning nothing |
| `0 latent overcrown` | not-yet pressure is held as structured zero | call every non-crown failure |
| `0 relation debt` | borrowed relation is held until ownership is earned | let relation echo become final `+1` |
| `0 return debt` | return theater is held until coherent return is earned | treat zero-crossing or collapse as return |
| `-1 false-one` | false pressure is visible and demoted | hide or crown traps |

## Required ablation enemies

The next proof line must compare the native final trinary witness against weaker alternatives:

- raw-expression-only witness;
- binary yes/no witness;
- no-return witness;
- no-relation witness;
- no-lineage / no-return-depth witness;
- no-echo-independence witness;
- no-zero-hold witness.

The native witness earns trust only if it beats these while preserving earned-one, holding structured zero pressure, and demoting false-one pressure.

## Weather order

```text
triad27 = 3^3 local expression weather
deep81  = 3^4 perturbation / late-shock bridge
wide243 = 3^5 temporal-depth / time-axis stress
```

Do not use bigger weather to hide local weakness. Four-corpus triad27 plus ablations comes first.

## Deferred bridge

External review, effective-dimension comparisons, DOI planning, and observed-universe bridge work remain deferred until the native controlled synthetic-field claim is coherent.

Observed-universe bridge rule:

> A match to known math or observed behavior is support for a modeling analogy, not proof that the universe uses ZeroGateSim.
