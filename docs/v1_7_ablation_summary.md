# v1.7.3-alpha — Ablation Summary

**Version:** `v1.7.3-alpha`  
**Purpose:** summarize what each ablation must expose before the v1.7 core question can close.
**Native witness:** `C_Z = min(D, P, R, B)`

## Three-part native win condition

The native final trinary witness must win three ways at once:

1. preserve earned-one;
2. hold structured zero/debt as visible `0`;
3. demote false-one pressure without final false-one crowns.

A baseline that only satisfies one of these is not enough. A dead-safe baseline can avoid false crowns by refusing everything. A raw baseline can preserve loud expression by crowning traps. A binary baseline can appear simple by destroying lane information. All three are cheap masks.

## Relation to earlier baseline work

The older native ablation baseline report remains part of the historical/recent evidence route. `v1.7.3-alpha` brings that pressure into the v1.7 core-question answer line and makes it explicit that baseline superiority is a required condition before closeout.

## Required output later in v1.7

Later evidence should show, in one table:

```text
native earned-one count
native structured-zero/debt count
native final false-one crowns
baseline earned-one count
baseline structured-zero promoted / erased
baseline final false-one crowns
baseline status
```

If any simpler witness explains the result equally well, the v1.7 core question cannot close `+1`.
