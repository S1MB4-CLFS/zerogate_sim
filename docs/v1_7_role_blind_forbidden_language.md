# v1.7.5-alpha — Role-Blind Forbidden Language

**Version:** `v1.7.5-alpha`  
**Native witness:** `C_Z = min(D, P, R, B)`  
**Core question:** can a final trinary witness distinguish earned-one from raw expression pressure, latent overcrown, relation debt, return debt, and false-one pressure under controlled synthetic-field adversarial weather?


## Forbidden after v1.7.5

Do not write:

```text
ZeroGateSim has solved role-blind false-one detection.
The witness independently discovers all false-one states.
The masked audit proves unlabeled unknown-field discovery.
Role masking removes designed-profile shaping.
```

## Allowed after v1.7.5 if the audit passes

Write:

```text
Under masked numeric review, the controlled designed-profile witness can keep core lane signals visible without direct role-label recounting.
```

## Why the boundary matters

A masked audit is a pressure test against role-label bookkeeping. It is not an independent generator challenge, not a holdout closeout, and not a role-blind discovery proof.

The later route remains:

```text
masked role-dependence audit -> fresh holdout synthetic-field challenge -> reviewer reproduction path -> core question closeout
```
