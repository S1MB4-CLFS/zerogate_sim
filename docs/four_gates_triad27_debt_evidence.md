# Four Gates Triad27 Debt Evidence

**Version:** `v1.6.20-alpha`  
**Status:** first repaired debt-evidence gate  
**Boundary:** no Zenodo route, no shadow revival, no observed-universe bridge, no spacetime metric claim

## Purpose

`v1.6.20-alpha` is the first heavy evidence gate for the debt-shaped candidates introduced in `v1.6.19-alpha`.

The active question is:

> Can a final trinary witness distinguish earned-one from raw expression pressure, latent overcrown, relation/return debt, and false-one pressure under controlled synthetic-field adversarial weather?

This version does not change the native witness:

```text
C_Z = min(D, P, R, B)
```

It changes what the evidence run asks the witness to face.

## What the report reads

The report combines two kinds of `triad27` evidence:

1. native four-gate adversary controls:
   - distinction;
   - polarity;
   - relation;
   - return;
2. one debt-shaped candidate matrix using candidate profile:

```text
four_gates_debt
```

The native adversary controls prove the witness still preserves earned-one, demotes false-one pressure, and wounds ablations. The debt candidate matrix asks whether zero can hold incomplete-but-meaningful becoming.

## Debt lanes

The report expects positive visibility for:

- `0 relation debt` — relation is meaningful but incomplete, unstable, under-owned, or global-only;
- `0 return debt` — return occurs but comes back changed, weakened, or closure-incomplete.

These lanes are not traps. They are near-success states.

## Pass boundary

A strong `v1.6.20-alpha` evidence result requires:

- earned-one preserved;
- false-one pressure demoted;
- final false-one crowns = `0`;
- relation debt visible;
- return debt visible;
- ablation enemies wounded;
- native witness unchanged.

If relation debt and return debt remain absent after this gate, the full debt claim must be demoted rather than patched forever.

## Forbidden interpretations

This report does not prove cosmology, physical dimensional genesis, quantum gravity, wormholes, or that reality uses the Four Gates. It only tests a controlled synthetic-field witness grammar.
