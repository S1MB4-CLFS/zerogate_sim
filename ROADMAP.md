# ZeroGateSim Roadmap

**Current line:** `v1.6.23-alpha` — history vault and current evidence surface cleanup  
**Release posture:** public research-alpha / controlled synthetic-field experiment line

ZeroGateSim now has a repaired Four Gates evidence path. The generated toy-field proof record is preserved in the history vault, not treated as the active claim surface. The roadmap should show what can still be earned, not replay every historical turn. Historical lanes live in [`docs/history_vault/README.md`](docs/history_vault/README.md) and detailed version truth lives in [`docs/version_truth.md`](docs/version_truth.md).

## North Star

The controlled synthetic-field language boundary remains active; this roadmap is not an observed-universe or physics-claim route.


ZeroGateSim tests whether dimensional expression can be modeled as candidate freedoms passing through four gates:

```text
Distinction -> Polarity -> Relation -> Return
```

The active research target is:

> Can a final trinary witness distinguish earned-one from raw expression pressure, latent overcrown, relation/return debt, and false-one pressure under controlled synthetic-field adversarial weather?

The current evidence line now shows the important trinary state shape:

```text
+1 earned-one visible
 0 relation debt visible
 0 return debt visible
-1 false-one pressure visible and demoted
final false-one crowns = 0
```

Native witness remains:

```text
C_Z = min(D, P, R, B)
```


## Pre-debt native stepping stones

These remain in the roadmap because they explain the current evidence route without reopening the full historical wall:

| version | role |
|---|---|
| `v1.6.14-alpha` | native four-gate claim audit |
| `v1.6.15-alpha` | native ablation baselines |
| `v1.6.16-alpha` | four-corpus triad27 native evidence |
| `v1.6.17-alpha` | deep81 / wide243 native evidence with debt lanes partial |

No new native gate was introduced in these steps; the native witness remains `C_Z = min(D, P, R, B)`.

## Current route

| version | status | role |
|---|---|---|
| `v1.6.18-alpha` | complete | Four Gates debt candidate design and logic cleanup |
| `v1.6.19-alpha` | complete | debt candidate generator |
| `v1.6.20-alpha` | complete | triad27 debt evidence: relation and return debt visible |
| `v1.6.21-alpha` | complete | deep81 / wide243 debt evidence: deeper weather preserved the pattern |
| `v1.6.22-alpha` | complete | fresh-seed debt reproduction: qualitative pattern reproduced; latent overcrown seed-sensitive |
| `v1.6.23-alpha` | current hygiene | history vault, public evidence surface cleanup, local runs archive plan |
| `v1.6.24-alpha` | planned package | manuscript / README correction package planning, not upload |
| `v1.6 closeout` | planned decision | +1 / 0 / -1 controlled synthetic-field claim decision |
| `v2.0` | deferred review release | stable external-review package, not observed-universe proof |

## Current evidence summary

| evidence gate | decision | important caveat |
|---|---|---|
| `v1.6.20` triad27 debt | expand | small-weather only |
| `v1.6.21` deep81 / wide243 debt | expand | some candidate families inactive |
| `v1.6.22` fresh-seed reproduction | expand | latent overcrown did not reproduce; relation/return debt did |

## v1.6.23-alpha — Evidence consolidation / history vault and evidence surface cleanup

Purpose:

- move historical clutter out of README / ROADMAP;
- preserve old proof, v1.5 controlled evidence, and shadow route work in `docs/history_vault/`;
- replace legacy evidence cards on README with current Four Gates debt evidence cards;
- provide a no-delete local `runs/` vault plan.

Boundary:

This is hygiene and consolidation, not a new scientific claim.

## v1.6.24-alpha — correction package planning

Purpose:

- prepare the manuscript / README correction package;
- produce a current evidence index;
- make the Zenodo new-version plan explicit but do not upload yet;
- separate simulation support, mathematical analogy, and physical speculation;
- preserve the caveat that latent overcrown is seed-sensitive in the repaired debt profile.

Pass condition:

The package must let a skeptical reader see exactly what was tested, where the result reproduced, and what remains unproven.

## v1.6 closeout decision

After v1.6.24, decide:

```text
+1 — controlled synthetic-field Four Gates witness claim earned enough for v2.0 review release planning.
 0 — partial result: earned-one / false-one / relation-return debt supported, but caveats block stronger package.
-1 — evidence route insufficient; demote claim and stop release path.
```

## v2.0 boundary

`v2.0` means stable external-review release package.

It does **not** mean:

- proof of physical dimensions;
- proof of cosmology;
- observed-universe bridge;
- quantum / wormhole / spacetime claim;
- role-blind discovery.

Deferred after v1.6 closeout:

- external reproduction of small deterministic runs;
- effective-dimension comparisons such as PCA rank, stable rank, graph diffusion behavior, and spectral-style return probabilities;
- literature comparison against emergence, pregeometry, effective dimension, and many-valued logic;
- evidence DOI plan;
- manuscript update separating simulation support, mathematical analogy, and physical speculation.

Observed-universe bridge rule:

> A match to known math or observed behavior is support for a modeling analogy, not proof that the universe uses ZeroGateSim.

## Historical material

The old release spine, shadow route, legacy evidence visuals, and README/ROADMAP snapshots are preserved in:

- [`docs/history_vault/README.md`](docs/history_vault/README.md)
- [`docs/version_truth.md`](docs/version_truth.md)

No deletion by vibes. No historical erasure. The vault keeps what was; the README shows what is; this roadmap shows what can be.
