# AGENTS.md — ZeroGateSim

## Project identity

ZeroGateSim is speculative research software for controlled synthetic-field
experiments. It does not establish physics, cosmology, dimensional origin,
clinical validity, or usefulness to Deep Temporal Affect.

The native historical witness is:

```text
C_Z = min(D, P, R, B)
```

Do not mutate that witness casually. A change to native mathematics requires a
separate version boundary and scientific checkpoint.

## Active operating protocol

Follow [`docs/UNIVERSAL_CODING_WORKFLOW_v3_CODEX_PROJECT.md`](docs/UNIVERSAL_CODING_WORKFLOW_v3_CODEX_PROJECT.md).

More access compresses mechanics; it does not collapse authority.

## Local execution

- Use the repository virtual environment and Python 3.12.
- Full test command:

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

- Run focused tests before the full suite.
- Preserve unrelated user changes and ignored historical run evidence.
- Use one primary writer. Subagents are read-only unless file ownership is
  explicitly divided.
- Generated evidence belongs under `runs/`, `reports/generated/`, or `exports/`
  unless a version deliberately promotes a small canonical artifact.
- Do not stage archives, virtual environments, caches, IDE state, or generated
  heavy evidence.

## Scientific gates

- Tests are evidence about code behavior, not truth.
- A scorer is not role-free if it can access `truth_role`, expected outcomes,
  designed labels, semantic candidate identity, or legacy role-derived outputs.
- Summary booleans such as `manifest_frozen=true` are not proof. Verify source
  artifacts and hashes or report `not_verified`.
- Zero false crowns cannot pass by itself. The evaluator must be capable of
  exposing an injected false crown, and always-HOLD must fail.
- Nested triad27/deep81/wide243 cases must not be pooled as independent evidence.
  Report unique cases, denominators, rates, overlap, and uncertainty.
- Prior-touch support is implemented as a development scorer, but it has no
  scientific authority until locked evaluation shows claim-relevant behavior
  and the final frozen verdict consumes it. Do not rename it continuous
  persistence.
- A failed frozen holdout is preserved and retired, not converted into a tuning
  set.

## Inherited v1.8.1 boundary

`v1.8.1-alpha` is the Lineage-Bearing Predictor Package and Development-Only
Plan Lock.

- Preserve `v1.7.11-alpha` as the latest released evidence-integrity correction
  and current scientific authority at `0 / HOLD`
  (`HOLD_CONSTRUCTION_BOUND`).
- Preserve merged `v1.8.0-alpha` as the exact observable-schema and label-firewall
  boundary; strengthen its numeric type refusal without changing its scientific
  status.
- The v1.8.1 predictor input is exactly three ordered frames (early, witness,
  late), each deriving its seven numeric fields from the v1.8.0 observable
  schema and binding that base schema ID/hash. Unknown, semantic, identifier,
  role, target, legacy verdict, and proxy fields fail closed at every frame.
- Call the implemented formula **prior-touch support**, not continuous
  persistence. It caps late owned pressure by the stronger early/witness touch.
  Dormant reappearance is allowed: a strong early touch may support a strong
  late frame across a lower witness frame.
- Keep opaque join identifiers outside the predictor callback.
- Freeze and hash predictions before any label join. Prediction and evaluation
  live in separate modules and separately serialized artifacts.
- Hash and parse the same immutable byte snapshot. Require both the expected
  prediction receipt and expected split-manifest hash before evaluation;
  the split manifest binds observables, join keys, and the label vault.
- Execute the predictor from the exact verified source-byte snapshot, not from
  mutable already-imported functions. In-process execution is still not an OS
  sandbox, and the coordinator is trusted as invoked.
- Require a caller-retained expected source-manifest hash, expected purpose,
  and allowed input root. Reject paths outside that root and declared
  label-informed or holdout construction. Treat source-manifest declarations as
  content-hash-bound declarations, not proof of unrestricted local history.
- Before downstream use, run the strict post-freeze verifier with independently
  retained expected package, source-manifest, and receipt hashes; recompute and
  compare score, manifest, and receipt bytes.
- Prove label-permutation and identifier-renaming invariance, tamper refusal,
  row-order consistency, exact joins, and evaluator failure capability with
  synthetic canaries only.
- Implement the threshold-free prior-touch score and no-prior-touch ablation;
  bind the base schema, frame schema, scorer source, coordinator, and
  configuration into a verified package.
- Do not build v1.8 lineage inputs from `evaluate_run`, `GateScores`, legacy
  zero bands, `endurance.py`, or `lineage.py`; those paths consume role-shaped
  verdict structure.
- Select no scientific threshold or abstention rule in v1.8.1. The selected
  option remains null.
- Preserve the complete v1.8.2 method lock: exact threshold boundaries; four
  named generator lineages with all three roles; nested leave-one-lineage-out
  folds; deterministic lexicographic selection; constant/simple baselines;
  no-prior-touch and no-echo ablations at frozen and retuned thresholds; exact
  denominators; lineage-cluster uncertainty; duplicate/aliasing and permutation
  rules; and fail-closed invalid/HOLD statuses.
- Do not alter `gates.py`, `earned_one.py`, or `final_output.py` in this repair
  sequence. Manuscript v2, DTA transfer, Zenodo, tags, and releases remain
  forbidden.

## Implemented v1.8.2 boundary

`v1.8.2-alpha` is the Failure-Capable Development Evaluation and Threshold
Selection gate. It may select one of the three v1.8.1-locked threshold options
using development labels only after all raw traces, frames, scores, options,
and model-by-option predictions are frozen and receipt-verified.

- Development generators are explicitly class-conditioned synthetic material;
  do not call them label-free or independent empirical data.
- Once raw traces are fixed, labels, blind IDs, and generator groups must not
  alter observable frames, model scores, threshold options, or precomputed
  predictions.
- Build exactly four distinct generator backends with 12 raw cases per role per
  backend. Generator lineage, not row, is the split and uncertainty unit.
- Use separate build-split, freeze-prelabel, and evaluate commands. Expected
  split and pre-label receipt hashes are mandatory caller inputs; no command may
  discover its own expected receipt.
- The post-label evaluator must not import or invoke generators, extractors, or
  predictors. It verifies frozen artifacts before its first label/group read.
- Report raw/effective denominators, duplicates, cross-lineage overlap,
  per-lineage metrics, lineage-macro rates, OOF predictions, baselines,
  frozen/retuned ablations, and deterministic cluster uncertainty.
- `READY_FOR_V1_8_3_CONTRACT_ONLY` is development progression, not scientific
  support. Baseline equivalence, prior-touch equivalence, dead-safe behavior,
  or failed failure canaries remain visible HOLD/INVALID outcomes.
- v1.8.3 holdout material does not exist yet and is not addressable by v1.8.2
  APIs. Do not scan the workspace or accept arbitrary input roots.

## Observed v1.8.2 outcome

The retained development decision is
`INVALID_CONSTANT_OR_DEAD_SAFE_PREDICTOR`; progression is false and the
selected threshold option is null.

- Integrity passed: 144 raw cases, 144 unique effective cases, four exact
  generator lineages, 12 cases per role per lineage, exact joins, no observable
  duplicates, and 6/6 production-routed failure canaries.
- The primary is variable globally (144 unique scores), but
  `piecewise_hysteresis_v1` has maximum score `0.58779880167953524` and therefore
  zero crowns at all locked crown minima (`0.8`, `0.7`, and `0.6`).
- No valid nested selection exists. Baseline, ablation, and cluster-uncertainty
  stages remain unexecuted; do not infer their result.
- Preserve `runs/v1_8_2_development_evaluation` as the retained local evidence
  root. Its evaluation receipt SHA-256 is
  `00935943039930113e732ea8f794adce0087458f2fc94d234285b089c440a205`.
- Do not lower thresholds, regenerate cases, or reinterpret the revealed
  development labels inside v1.8.2. A repair requires a new preregistered
  version boundary and untouched development material.
- v1.8.3 and v1.8.4 are blocked. Do not generate, freeze, or reveal holdout
  material from this invalid prerequisite.

Every version updates `README.md`, `ROADMAP.md`, `docs/version_truth.md`, current
evidence surfaces, the claim boundary, release note, history pointer, and one
central version-surface test.

## Coding economics default

Codex works through local green. At the checkpoint, prefer a manual handoff for
CI review, PR merge, branch deletion, and optional release mechanics. Codex must
provide exact commands or URLs and re-baseline after any manual code or evidence
edit. Do not spend agent time polling CI unless explicitly requested.

The current v1.8.1-through-v1.8.4 sequence is a recorded workflow-research
exception: the user intentionally authorized full-agent GitHub and scientific
execution to observe time savings and quality. Record cycles, review findings,
CI/merge receipts, human intervention, and user-supplied credit context in
`docs/workflow_research_ledger.md`.

Workflow-research mode may intentionally delegate deterministic Git/CI work
that would be cheaper for the human to click manually. Record that choice as an
observation cost, not as the permanent default and never as permission to work
past a failed scientific gate.

## Authority

For this named sequence, the user has explicitly authorized Codex through
`v1.8.4-alpha` to:

- implement, test, independently review, and generate bounded evidence;
- select scientific decision thresholds from the locked v1.8.2 development
  data and method only;
- build and freeze the v1.8.3 unseen-generator holdout contract and
  predictions;
- perform the v1.8.4 one-pass label join and artifact-generated closeout;
- create local commits, push branches, open PRs, inspect and repair CI, and
  merge each version.

This authorization does not require advancing past a failed gate. Preserve and
obey `INVALID`, `HOLD`, or `FALSIFIED` outcomes; never retune on holdout labels.
The v1.8.2 invalid result has now triggered that stop. The authorization remains
a historical mechanics boundary; it does not authorize v1.8.3 or v1.8.4 until
a new valid prerequisite is earned.

Still forbidden without new explicit authority:

- tags and releases;
- DTA transfer or integration;
- manuscript v2 prose;
- Zenodo or other archive publication;
- destructive recovery or scope expansion;
- email or other external communication. Never send email; draft only when
  asked.

At each checkpoint, suggest the exact safest next prompt.
