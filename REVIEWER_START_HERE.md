# Reviewer Start Here — ZeroGateSim

**Current development line:** `v1.8.2-alpha` — Failure-Capable Development Evaluation

**Scientific authority:** `v1.7.11-alpha` — Evidence Integrity Correction

**Scientific decision:** `0 / HOLD`
**Native witness:** `C_Z = min(D, P, R, B)`
**Boundary:** controlled synthetic-field research software only.

This is the narrow door for a cold reviewer.

The `v1.7.9-alpha` Reviewer Start Here / Reproduction Package remains the
historical packaging foundation; v1.7.11 changes its scientific interpretation.

Read in this order:

```text
README.md
-> docs/v1_8_2_failure_capable_development_evaluation.md
-> docs/v1_8_1_lineage_predictor_package.md
-> docs/v1_8_observable_schema_label_firewall.md
-> docs/v1_7_11_evidence_integrity_correction.md
-> docs/v1_7_claim_boundary_card.md
-> docs/v1_7_latest_holdout_snapshot.md
-> docs/v1_7_minimal_reproduction.md
-> docs/v1_7_evidence_manifest.md
-> docs/v1_7_anti_tautology_role_dependence_check.md (historical / superseded)
```

## What is being reviewed

The current question is not whether the old artifacts can be regenerated. They
can. The question is whether they support blind empirical discrimination.

They do not yet:

- the current final path consumes truth role;
- the existing anti-tautology audit trusted summary declarations;
- triad27, deep81, and wide243 are nested views rather than independent evidence;
- lineage is emitted beside the final verdict rather than consumed by it.

`v1.7.11-alpha` therefore reopens the core question at `0 / HOLD`.

`v1.8.0-alpha` repairs the callback-argument/schema and local hash-integrity
boundary and is locally green. It does not change that scientific answer: its
scorer is a synthetic canary, no scientific thresholds are selected, and no
frozen holdout is revealed.

`v1.8.1-alpha` adds an ordered early/witness/late **prior-touch support** path.
It does not claim continuous persistence: a strong early touch may support a
late reappearance across a lower witness frame. The three-frame schema derives
from and hash-binds the v1.8.0 schema, and predictor execution is loaded from
the verified source-byte snapshot rather than mutable imported functions.

Score freezes require an allowed-root source manifest and caller-retained
expected hashes, then a strict verifier recomputes the score table, manifest,
and receipt. The source manifest's no-label/no-holdout statements are
content-bound declarations, not external proof of unrestricted history.
Synthetic ablation and formula-input canaries show implemented mechanism
behavior, not empirical accuracy. The selected threshold remains null.

v1.8.2 executed that byte-locked method on class-conditioned controlled
synthetic development material. The 144 raw cases remained 144 effective
unique cases after exact joins and a zero-duplicate audit, and all six
failure-capability fixtures passed. The primary score was nonconstant across
the corpus, but the complete `piecewise_hysteresis_v1` lineage had a maximum
score of `0.58779880167953524`. It therefore produced no crowns under any of
the locked `.8`, `.7`, or `.6` crown boundaries.

The exact result is `INVALID_CONSTANT_OR_DEAD_SAFE_PREDICTOR`, with reason
`no valid locked threshold option; piecewise_hysteresis_v1:INVALID_DEAD_SAFE_NO_CROWNS`.
The selected option is `null`; uncertainty, baseline comparison, and ablation
comparison correctly did not execute because valid nested selection was
impossible. `READY_FOR_V1_8_3_CONTRACT_ONLY` was not earned, and no v1.8.3 or
v1.8.4 execution is authorized by this result.

## Small software reproduction

Run:

```powershell
powershell -ExecutionPolicy Bypass -File ./scripts/run_v1_7_small_reproduction.ps1
```

This reproduces historical role-aware software behavior and packaging. It does
not close the corrected scientific question.

## Corrected integrity audit

Run the new CLI against the three rung-specific matrix roots:

```powershell
.\.venv\Scripts\python.exe -m zerogate_sim.v1_7_evidence_integrity_correction `
  --triad-root <triad27-matrix-root> `
  --deep-root <deep81-matrix-root> `
  --wide-root <wide243-matrix-root> `
  --out runs\v1_7_11_evidence_integrity_correction
```

The audit derives atomic case identity, source hashes, overlap, denominators,
rates, and nested-safe totals. Missing metadata or malformed counts fail closed.

## Heavy evidence policy

The rung order remains useful for inspection:

```text
triad27 -> inspect -> deep81 -> inspect -> wide243 -> inspect -> atomic overlap audit
```

Do not sum the rungs as independent evidence. The apparent 375,921 opportunities
collapse to a unique union of 260,253 because the narrow cases are exact members
of the wider views.

## Current boundary

The strongest supported statement is:

> The legacy role-aware controlled harness reproducibly partitions its designed lanes.

Do not read this package as:

- a current `+1` core-question closeout;
- blind or role-free false-one detection;
- independent generator validation;
- a valid selected scientific threshold or evidence that prior-touch support
  improves the final frozen verdict;
- permission for manuscript v2 or DTA transfer;
- physics, cosmology, dimensional-origin, clinical, or observed-universe proof.
